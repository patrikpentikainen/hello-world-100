# C# – Hello World

## English 🇬🇧

This folder contains a simple **Hello World** program written in **C#** using the modern **.NET SDK**.
The example is part of the **HelloWorld100** project, which demonstrates a minimal **Hello World** program implemented in multiple programming languages.

### Files

- `hello.cs` – C# source file that prints `Hello World!`
- `HelloWorld.csproj` – .NET project file used for building and running the program
- `HelloWorld.sln` – Solution file for IDE support (VS Code / Visual Studio)

### Requirements

- .NET SDK **8.0 or newer**
- Windows, Linux, or macOS
- Command-line terminal (PowerShell, Bash, etc.)

### Run the program

Make sure you are in the `csharp` directory where the `.csproj` file is located.
Navigate to the `csharp` directory and run:

```powershell
dotnet run
```

### Output

```text
Hello World!
```

### What this example demonstrates

- Basic structure of a C# console application
- Use of the `Main` method as the program entry point
- Writing output to the console using `Console.WriteLine`
- Building and running a C# project with the `dotnet` CLI

---

## Suomi 🇫🇮

Tämä kansio sisältää yksinkertaisen **Hello World** -ohjelman, joka on kirjoitettu **C#**-kielellä käyttäen modernia **.NET SDK**:ta.
Esimerkki on osa **HelloWorld100**-projektia, jonka tarkoituksena on näyttää sama **Hello World** -ohjelma toteutettuna useilla eri ohjelmointikielillä.

### Tiedostot

- `hello.cs` – C#-lähdekoodi, joka tulostaa tekstin `Hello World!`
- `HelloWorld.csproj` – .NET-projektitiedosto ohjelman kääntämistä ja ajamista varten
- `HelloWorld.sln` – Ratkaisutiedosto IDE-tukea varten (VS Code / Visual Studio)

### Vaatimukset

- .NET SDK **8.0 tai uudempi**
- Windows, Linux tai macOS
- Komentorivi (PowerShell, Bash tms.)

### Ohjelman ajaminen

Varmista, että olet `csharp`-kansiossa, jossa `.csproj`-tiedosto sijaitsee.
Siirry `csharp`-kansioon ja suorita:

```powershell
dotnet run
```

### Tuloste

```text
Hello World!
```

### Mitä tämä esimerkki havainnollistaa

- C#-konsoliohjelman perusrakenteen
- `Main`-metodin roolin ohjelman aloituspisteenä
- Tulostuksen konsoliin `Console.WriteLine`-komennolla
- C#-projektin kääntämisen ja ajamisen `dotnet`-komentorivityökalulla

---
